# 更新日志 — 文档召回重排功能

> 日期：2026-07-24

## 功能概述

在 RAG 检索流程中增加了**重排（Rerank）**环节：向量检索召回候选文档后，使用阿里云百炼平台的重排模型（`gte-rerank`）对文档进行语义精排，将最相关的文档排在前面，再送入 LLM。

## 设计原理

### 为什么需要重排

原流程：
```
用户查询 → Milvus 向量检索 (L2距离) → Top-3 文档 → 直接给 LLM
```

向量检索（embedding）用固定维度的向量表示整篇文档，不可避免会有**信息压缩损失**。Embedding 相似度高 ≠ 真正语义相关。例如：
- 查询："如何优化 CPU 使用率"
- 向量检索可能召回一篇讲"CPU 架构"的文档（词面上 CPU 出现多），实际并不解答"如何优化"

**重排模型（Cross-Encoder）**将 `(query, document)` 成对输入 Transformer，做完整的交叉注意力计算，能更精准地判断相关性——代价是计算量更大，不适合海量候选。

### 新流程：召回 → 粗排 → 精排

```
用户查询 → Milvus 向量检索 (10篇候选) → 百炼重排模型精排 → Top-3 → LLM
              ↑ 快速但粗糙                    ↑ 慢但精准
```

这是一个经典的**两阶段检索**策略：
1. **粗排（召回）**：用 Milvus 向量检索从海量文档中快速缩小范围到 10 篇
2. **精排（重排）**：用重排模型对这 10 篇逐篇打分，选最相关的 3 篇

在不增加最终上下文长度（仍是 3 篇）的前提下，显著提升检索精度。

### 为什么是 gte-rerank

`gte-rerank` 是阿里云百炼平台提供的通用文本重排模型（Generalized Text Embedding - Rerank），基于 Qwen 架构，中文场景表现优秀。项目已有 `dashscope` SDK 依赖，零额外安装成本。

## 改动文件清单

| 操作 | 文件 | 说明 |
|---|---|---|
| **新增** | `app/services/rerank_service.py` | 重排服务，封装 DashScope TextReRank API |
| **修改** | `app/config.py` | 新增 4 个重排配置项 |
| **修改** | `app/tools/knowledge_tool.py` | 检索流程插入重排步骤 |
| **修改** | `.env.example` | 新增重排环境变量模板 |

## 各文件改动详解

### 1. `app/services/rerank_service.py`（新增）

**`RerankService` 类**，单例模式，提供两个核心方法：

- `rerank(query, documents, top_n)` → 调用 DashScope TextReRank API，返回 `[(原始索引, 分数)]` 按分数降序
- `rerank_documents(query, docs, top_n)` → 面向 LangChain `Document` 对象的便捷封装

**关键实现细节：**
- 使用 `dashscope.TextReRank.call()`，模型默认 `gte-rerank-v2`（`gte-rerank` v1 需在百炼控制台手动开通）
- API Key 从 `config.dashscope_api_key` 读取（复用已有 DashScope 密钥）
- **失败不降级**：API 返回非 200 时直接 `raise RuntimeError`
- 仅 1 篇文档时跳过高开销重排，直接返回
- 全局单例 `rerank_service`，与项目其他 service 风格一致

**API 请求格式（SDK 自动处理）：**
```json
{
  "model": "gte-rerank",
  "input": {
    "query": "如何优化 CPU 使用率",
    "documents": ["文档1内容...", "文档2内容..."]
  },
  "parameters": {
    "top_n": 3,
    "return_documents": false
  }
}
```

**响应中每个 result 包含：**
- `index`：原始文档在输入列表中的索引
- `relevance_score`：0~1 之间的相关性分数

### 2. `app/config.py`（修改）

新增 4 个 Pydantic Settings 配置项：

```python
# 重排 (Rerank) 配置
rerank_enabled: bool = True           # 是否启用重排，可通过环境变量关闭
rerank_model: str = "gte-rerank"      # 重排模型名称
rerank_retrieve_n: int = 10           # 召回阶段获取的候选文档数
rerank_top_k: int = 3                 # 重排后保留的文档数
```

**配置逻辑：**
- `rerank_enabled=True` 时：召回 `rerank_retrieve_n` 篇（默认 10），重排后取前 `rerank_top_k` 篇（默认 3）
- `rerank_enabled=False` 时：回退到原逻辑，召回 `rag_top_k` 篇（默认 3），不重排
- 所有值可通过 `.env` 文件或环境变量覆盖

### 3. `app/tools/knowledge_tool.py`（修改）

`retrieve_knowledge()` 函数的检索流程从原来的 3 步变为 4 步：

```python
# 原流程（2 步）
docs = retriever.invoke(query)        # k = rag_top_k (3)
context = format_docs(docs)

# 新流程（4 步）
docs = retriever.invoke(query)        # k = rerank_retrieve_n (10)  ← 召回更多
docs = rerank_service.rerank_documents(query, docs)                ← 新增：重排
docs = docs[:rerank_top_k]            # 取前 3                       ← 截断
context = format_docs(docs)
```

**向后兼容：**
- 当 `rerank_enabled=False` 时，行为与原流程完全一致
- 当 `rerank_enabled=True` 但召回文档仅 1 篇时，跳过重排（无需重排单篇文档）

**错误处理变更：**
- 原代码 catch 所有异常并返回字符串，等于静默降级
- 改为 `raise` 直接抛出，让重排失败对调用方可见（符合"不降级"要求）
- 调用方（Agent 框架 / AIOps Planner）有各自的异常处理，不会导致系统崩溃

### 4. `.env.example`（修改）

新增配置模板：
```ini
# 重排 (Rerank) 配置
RERANK_ENABLED=True
RERANK_MODEL=gte-rerank
RERANK_RETRIEVE_N=10
RERANK_TOP_K=3
```

## 影响范围

### 受益的调用路径

所有使用 `retrieve_knowledge` 的地方自动获得重排能力：

1. **RAG 对话**：`POST /api/chat` 和 `/api/chat_stream` → Agent 调用 `retrieve_knowledge` 工具时，自动享受精排
2. **AIOps 诊断**：`POST /api/aiops` → Planner 节点调用 `retrieve_knowledge.ainvoke()` 获取经验文档时，自动享受精排

### API 消耗

每次重排调用消耗 DashScope token（按文档总字符数计费）。以默认配置为例：
- 每次检索调用重排 1 次
- 输入：query + 10 篇文档文本
- 建议根据实际文档长度调整 `rerank_retrieve_n`（候选太多浪费 token，太少精排效果差）

## 配置调优建议

| 场景 | rerank_retrieve_n | rerank_top_k | 说明 |
|---|---|---|---|
| 知识库文档少（<50篇） | 5 | 3 | 候选不需要太多 |
| 知识库文档多（>200篇） | 10~15 | 3 | 多召回候选提升精排命中率 |
| 长文档场景 | 5 | 3 | 减少 token 消耗 |
| 关闭重排 | — | — | `RERANK_ENABLED=False` |

## 验证方式

1. 确保 `.env` 中 `DASHSCOPE_API_KEY` 已配置有效密钥
2. 确保 `.env` 中 `RERANK_ENABLED=True`
3. 启动项目：`python -m app.main`
4. 发送一条需要检索知识库的对话，观察日志：
   ```
   知识检索工具被调用: query='...'
   向量检索召回 10 个候选文档
   启用重排，调用百炼重排模型...
   重排完成: 返回 10 个结果, 最高分=0.8732, 最低分=0.1245
   最终返回 3 个文档（重排=启用）
   ```
5. 对比关闭重排（`RERANK_ENABLED=False`）时返回的文档顺序，确认精排有效果
